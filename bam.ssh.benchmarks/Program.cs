namespace Bam.Ssh.Benchmarks;

/// <summary>
/// Entry point for the bam.ssh benchmark suite. Benchmark scenarios are added
/// during the performance optimization phase of the implementation plan.
/// </summary>
public static class Program
{
    /// <summary>
    /// Runs the benchmark suite.
    /// </summary>
    public static void Main(string[] args)
    {
        Console.WriteLine("bam.ssh benchmarks: no benchmark scenarios are registered yet (added in the performance phase).");
    }
}
