namespace Bam.Ssh.Samples;

/// <summary>
/// Entry point for the bam.ssh console samples. Sample scenarios are added as
/// the corresponding client and server features land.
/// </summary>
public static class Program
{
    /// <summary>
    /// Runs the sample selector.
    /// </summary>
    public static void Main(string[] args)
    {
        Console.WriteLine("bam.ssh samples: sample scenarios are added as client and server features land.");
    }
}
